# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtualenv
.venv
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Env & secrets
.env
.env.*
*.pem
*.key
secrets/

# Drive partitions (sovereign rule)
RAW_BACKUPS/
WORKING_BRAIN/
artifacts/large/
*.tar.gz
*.zip

# Google Drive sync / temp files
.tmp.driveupload/
.tmp.drive.upload/
tmp.drive.upload/

# Grok Build specific
state.json
phase_progress.json
*.log
grok_build.egg-info/

# OS
.DS_Store
Thumbs.db

# Temp test outputs
tests/output/

# Large data source (never commit the 1GB export)
prod-grok-backend.json

# Generated large artifacts - we keep structure but can ignore data if too big
valerie_out/pre_extract/**/*.json
valerie_out/pre_extract/**/*.md
valerie_out/pre_extract/**/*.txt
# But for now allow small ones for progress; adjust later

# Keep the pre_extract structure for git but ignore very large per-day files if needed
# valerie_out/pre_extract/

valerie_out/*.jsonl
valerie_out/daily_enriched/

# Python packaging
grok_build.egg-info/
*.egg-info/

# IDE / editor
.vscode/
.idea/
.vs/
*.swp
prod-grok-backend.json
